"use client";

// import { useSession } from "next-auth/react";

export default function InvitesPage() {
	// const session = useSession();

	// if (!session) return <>not logged</>;

	return <div className="space-y-6">Invites!</div>;
}
